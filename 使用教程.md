# 使用教程：下载、打开、开始用

DeepSeek Harness macOS 是把 [DeepSeek Harness（dsh）](https://www.npmjs.com/package/@deepseek-ai/dsh) 网页版完整打包的 macOS 桌面 App。**App 内部自带了 Node.js 和 dsh**，不需要你安装任何东西，也不需要打开终端。

---

## 第一步：下载 App

去 Releases 页面：<https://github.com/Nick-Job/deepseek-harness-macos/releases>

下载最新版本，二选一：

- **`DeepSeek Harness.app.zip`**：解压后得到一个 `DeepSeek Harness.app`，双击就能用
- **`DeepSeek Harness.dmg`**：双击挂载后，把 App 拖进「应用程序」

> 下载页还有一个 `version.json`，用来记录这一版 App 里内置的是哪一版 dsh，遇到问题时可一并附上。

## 第二步：打开 App

直接**双击** `DeepSeek Harness.app` 即可，完全不需要终端，**也不会弹出浏览器**。

App 会自动帮你完成剩下的事：

1. 挑运行时：优先用自动更新装上的新版运行时，其次 App 内置的运行时
2. 检查 `127.0.0.1:3080` 是否已有 dsh web 在运行
3. 没有的话，用内置的 Node.js + dsh 自动启动服务
4. 等首页真的能打开后，才显示独立窗口加载界面
5. 退出 App 时，自动停掉本次启动的服务，不留后台进程

> 💡 首次打开会初始化 dsh 的配置文件（在你的用户目录下），比平时慢几秒是正常的。

## 版本会自己更新，不用重新下载 App

dsh 上游更新很勤（经常一天一个 rc），所以这个桌面 App 会自己盯着：

- **打开 App 时**，后台会核查一次上游版本（默认 12 小时一次）
- 发现内置的 dsh 不是最新版，就用 App 自带的 node/npm 把它**装着更新到本机**（装在 `~/Library/Application Support/DeepSeek Harness/`，不动 App 本身）
- 装完会自动验收：首页必须能直接打开、且全程不会弹浏览器；**验收通过才启用，下次打开就是新版**
- 验收失败或没网，就继续用原来的版本，完全不影响你使用
- 如果 App 外壳本身有新版本（需要重新下载安装包），会弹一次提示，点「打开发布页」即可

想知道现在跑的是哪一版？看日志第一行：

```
[2026-09-11 10:00:00] 运行时: 热更新运行时 (...), dsh 0.1.5-rc.1 (App 1.2.0)
```

## 第一次打开提示"无法验证开发者"怎么办

因为 App 是 ad-hoc 签名、没有做苹果公证，macOS 第一次会拦截。解决办法：

1. 在「访达」里找到 `DeepSeek Harness.app`
2. **右键** 它 → 选择 **打开**
3. 弹出提示后，再点一次 **打开** 即可

只需操作这一次，之后都能正常双击打开。

## 常见问题

| 问题 | 解决办法 |
| --- | --- |
| 提示"无法验证开发者" | 右键 App → 打开（见上文，只需一次） |
| 提示"已损坏，无法打开" | 与上一条同理，右键 → 打开；或在「系统设置 → 隐私与安全性」底部允许 |
| 打开后一直是空白页 | 等几秒让服务就绪；仍不行就退出重开 |
| 打开后窗口立即闪退 | 看日志 `~/Library/Logs/deepseek-harness-dsh.log`，并把内容发到 Issues |
| 双击后弹出了浏览器 | 正常版本不会。日志里搜 `open-guard` 能看到是否被拦截；若确实被打开，请把日志发到 Issues |
| 提示 `dsh web authentication required` | 说明有别的 `dsh web` 占着 `3080`（比如终端里另开的），先退掉它再开 App |
| 更新后界面报错/打不开 | 说明新版本不合适：彻底退出 App，删掉 `~/Library/Application Support/DeepSeek Harness/runtime` 这个目录，再打开 App 就会用回内置版本 |

## 高级：想自己构建

见仓库 [README.md](README.md) 的「本地构建」一节，或直接使用 GitHub Actions 构建。

---

## 日志位置

App 的启动日志在：

```
~/Library/Logs/deepseek-harness-dsh.log
```

里面会记录：用了哪个运行时、内置 dsh 版本、上游核查结果、热更新是否成功、以及浏览器被拦截的记录。遇到问题时可查看此文件，或把内容贴到 [Issues](https://github.com/Nick-Job/deepseek-harness-macos/issues)。
