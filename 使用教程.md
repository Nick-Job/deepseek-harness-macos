# 使用教程：从安装到打开 App

DeepSeek Harness macOS 是 [DeepSeek Harness（dsh）](https://www.npmjs.com/package/@deepseek-ai/dsh) 网页版的桌面壳。它本身不包含数据，**必须先在本机启动 dsh web 服务，再打开 App**，App 才会显示界面。

---

## 第一步：安装 DeepSeek Harness（dsh）

App 需要 Node.js 20 或更高版本（推荐 22）。没装 Node 的话，先去 [nodejs.org](https://nodejs.org) 下载安装。

安装 dsh，任选一种方式：

```bash
# 方式 A：全局安装（推荐，之后直接敲 dsh 即可）
npm install -g @deepseek-ai/dsh

# 方式 B：免安装，临时用 npx
npx @deepseek-ai/dsh web
```

验证是否装好：

```bash
dsh --version
# 例如输出：0.1.0-rc.6
```

---

## 第二步：在终端启动 dsh web

打开「终端」（Terminal / iTerm），运行：

```bash
dsh web
```

- 首次运行会自动初始化 web profile，稍等片刻即可
- 服务启动后默认监听 **`http://127.0.0.1:3080`**
- **保持这个终端窗口开着**；想退出时按 `Ctrl+C`

（可选）换端口启动：

```bash
dsh web --port 8080
```

> ⚠️ 注意：打包好的 App 固定访问 3080 端口。如果用 `--port` 改了端口，请在浏览器里访问对应地址，或重新打包一份对应端口的 App。

---

## 第三步：打开 DeepSeek Harness App

先确认服务正常：用浏览器打开 `http://127.0.0.1:3080`，能看到界面即可。

然后任选一种方式打开 App：

- **压缩包版**：解压 `DeepSeek Harness.app.zip`，双击里面的 `DeepSeek Harness.app`
- **DMG 安装版**：双击 `DeepSeek Harness.dmg`，把 App 拖进「应用程序」，再从启动台打开

App 会打开一个独立窗口加载 `http://127.0.0.1:3080`，界面和浏览器里看到的一样。

---

## 常见问题

| 问题 | 解决办法 |
| --- | --- |
| 窗口是空白页 | dsh web 没启动，或端口不是 3080。回到终端确认 `dsh web` 正在运行 |
| 提示“无法验证开发者” | 右键 App →「打开」。当前为 ad-hoc 签名，未做公证 |
| 端口被占用 | 换端口：`dsh web --port 8080`，或先停掉占用 3080 的程序 |
| 打开后提示无法连接 | 终端里的 dsh web 退出了，重新运行 `dsh web` 再打开 App |
| 想看最新界面 | App 只是网页的壳，内容实时来自 dsh web，刷新即可 |

---

## 下载

所有安装包都在 Releases 页面：<https://github.com/Nick-Job/deepseek-harness-macos/releases>
